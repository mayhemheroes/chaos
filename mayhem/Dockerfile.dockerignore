# BuildKit per-Dockerfile ignore (applies when building with -f mayhem/Dockerfile).
# Keep .git — the commit image must carry the full history. Drop only local build cruft.
build-fuzz/
build-san/
build-test/
chaos
chaos-standalone
parser.tab.c
parser.tab.h
lex.yy.c
*.o
.DS_Store
*.swp
*~
